# Vivekananda Student App (backend)

This repo contains the backend for the Vivekananda Student App (FastAPI) along with local docker-compose for development and a GitHub Actions workflow to build & deploy to Cloud Run.

See .env.example for local environment variables.
